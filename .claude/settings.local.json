{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git config *)",
      "Bash(git commit *)",
      "Bash(git remote *)",
      "Bash(git push *)",
      "PowerShell(cd \"C:\\\\Users\\\\AdhamHElSayed\\\\Desktop\\\\Claude Test Project 1\"; $tag = \"snapshot-$\\(Get-Date -Format 'yyyy-MM-dd'\\)\"; $exists = git tag -l $tag; if \\($exists\\) { Write-Output \"Tag $tag already exists, skipping.\" } else { git tag $tag; Write-Output \"Tag $tag created.\" })",
      "PowerShell(cd \"C:\\\\Users\\\\AdhamHElSayed\\\\Desktop\\\\Claude Test Project 1\"; git commit -m \"Daily snapshot - $\\(Get-Date -Format 'yyyy-MM-dd'\\)\")",
      "Bash(Get-Date -Format 'yyyy-MM-dd')",
      "PowerShell(cd \"C:\\\\Users\\\\AdhamHElSayed\\\\Desktop\\\\Claude Test Project 1\"; $date = Get-Date -Format 'yyyy-MM-dd'; $tag = \"snapshot-$date\"; $existingTag = git tag -l $tag; if \\($existingTag\\) { Write-Host \"Tag $tag already exists, skipping.\" } else { git tag $tag; Write-Host \"Created tag: $tag\" })"
    ]
  }
}
